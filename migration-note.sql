ALTER TABLE subdomains ADD COLUMN note TEXT;

-- ============================================================================
-- 校验（执行完上面那条后，单独再跑这一条）：
--   SELECT id, name, note FROM subdomains LIMIT 5;
-- ============================================================================
--
-- ⚠️ 注意：本文件故意把可执行语句写成一整行、且放在最前面。
-- D1 Console 粘贴时可能把换行压成一行，而 SQLite 的 `--` 是「注释到行尾」，
-- 一旦整个脚本变成一行，第一个 `--` 就会把后面全部内容注释掉，报
-- `incomplete input: SQLITE_ERROR` —— 看着像语法错，其实是内容被吃掉了。
--
-- ============================================================================
-- 迁移 7：卡槽备注（用户给自己的子域名起名字）
-- ============================================================================
--
-- 字段说明：
--   note   最长 20 个字符；NULL 表示没有备注。纯用户侧整理标签，
--          不参与 DNS 解析，也不要求唯一 —— 两个卡槽可以叫同一个名字。
--
-- 未执行本迁移时平台不会报错：
--   * 卡槽列表与管理员列表都单独 try/catch 查这一列，失败就当作没有备注；
--   * 设置备注的接口会返回明确提示，让你先执行本迁移。
--
-- 用 wrangler 跑本文件时换行不会被压，可直接用：
--   npx wrangler d1 execute fblog-dns-db --remote --file=./migration-note.sql