# 安全政策

本文件说明如何向 **fblog.cyou 免费二级域名分发平台**（下称「本项目」）报告安全问题。

本项目是一个**免费、非商业、按「现状」提供**的公益服务，不承诺任何服务水平（SLA）。但安全漏洞会被认真对待。

---

## 一、如何报告

**请不要开公开 issue。** 公开披露会让漏洞在修复前就被利用 —— 本平台持有用户账号与域名解析权限，影响面比一般项目大。

请优先使用 GitHub 的**私密漏洞报告**功能：

> 仓库页面 → **Security** 标签 → **Report a vulnerability**

若该功能不可用，可通过站长博客所载联系方式与维护者取得联系。

### 报告请包含

- 漏洞类型与影响范围（例如：能读到他人数据 / 能改他人域名解析 / 能绕过鉴权）
- 复现步骤（越具体越好，含请求与响应）
- 受影响的文件或接口路径
- 你评估的严重程度，以及是否已有公开披露计划

### 响应预期

这是一个个人维护的公益项目，**没有专职安全团队**，请理解响应可能不快。维护者会尽力：

| 阶段 | 期望 |
| --- | --- |
| 确认收到 | 7 天内 |
| 初步评估 | 14 天内 |
| 修复或给出处置方案 | 视严重程度，尽量 30 天内 |

修复发布后，如果你愿意，会在本文件或 CHANGELOG 中致谢（匿名亦可）。

---

## 二、范围内的漏洞（欢迎报告）

| 类别 | 具体举例 |
| --- | --- |
| **鉴权与越权** | 绕过登录；伪造或篡改会话 Cookie；普通用户访问管理端接口；修改、删除**他人**的卡槽 / 解析记录 / 反代配置 |
| **卡槽与配额绕过** | 绕过卡槽上限；通过多账号或竞态条件超额申请；骗取 GitHub 星标解锁权益 |
| **注册与验证码** | 绕过邮箱验证直接建号；验证码可爆破或预测；验证码可被他人窃取；重置他人密码 |
| **反向代理** | 绕过反代目标白名单；把反代用作任意第三方站点的开放代理；SSRF；通过构造 Host / 请求头把流量转发到非预期目标 |
| **注入与数据处理** | SQL 注入；在子域名、备注、反代目标等用户可控字段中构造出越权行为 |
| **凭据泄漏** | 仓库、前端产物或错误信息中泄漏 API Token / 管理员口令 / 会话密钥 |
| **邮件通道** | 把本平台当作垃圾邮件发送通道；滥用邮件接口 |
| **DDNS** | 用他人的 DDNS 令牌更新记录；令牌可被枚举或预测 |

---

## 三、不在范围内（请勿报告）

以下属于**已知的设计取舍或第三方问题**，不是漏洞：

- **限流额度是「按 Cloudflare 机房」计数**，不是全局精确限流 —— 这是官方机制的粒度限制，已在 README 中说明。请求被 anycast 分散到多个机房时，实际允许量高于配置值，属于预期行为。
- **Workers 免费额度 100,000 请求/天**、邮件服务免费额度等上游配额限制。
- **Cloudflare / Resend / Brevo / 阿里云 / 腾讯云等第三方平台自身的问题** —— 请向对应厂商报告。
- **用户自己站点的漏洞** —— 本平台只做解析与转发，不参与、不审核用户站点的内容与实现。
- **用户站点承载的违规内容** —— 属于滥用举报，请按《[使用规则](https://dns.fblog.cyou/rules)》的举报流程处理，不走本文件。
- **缺少某项安全加固建议**（如未启用某响应头、未做某编码）而无实际可利用影响 —— 欢迎提正常 issue 讨论，不算安全报告。
- 自动化扫描器输出的**无 PoC 的告警**。

---

## 四、本项目自身的安全设计

供报告前参考，以下机制**已经存在**，若能绕过才构成漏洞：

- 会话使用 HMAC 签名的 HttpOnly Cookie，密钥为 `SESSION_SECRET`
- 管理端接口统一经 `requireAdmin` 校验
- 反代目标使用**硬编码后缀白名单**（`*.workers.dev`、`*.pages.dev`、`*.fcapp.run`、`*.tencentscf.com`），无环境变量开口子
- 邮箱验证码经哈希后存储，带有效期、尝试次数与发送频率限制
- 卡槽归属在每次读写时按 `user_id` 校验，不信任前端传入的 ID
- 管理员口令比较使用**恒定时间**比较
- 所有密钥通过 `wrangler secret` 注入，不进入代码仓库

---

## 五、维护者请知悉

若你 fork 本项目建设自己的平台，请务必：

1. 更换全部密钥（`CF_API_TOKEN`、`CF_ZONE_ID`、`ADMIN_PASSWORD`、`SESSION_SECRET`），**不要复用本文档所在仓库的任何值**
2. 确认 `.dev.vars` 未被提交（本项目已在 `.gitignore` 中排除）
3. 在 GitHub 仓库设置中开启 **Secret scanning** 与 **Push protection**
4. 本项目以 **AGPL-3.0** 授权：若你将其作为网络服务对外提供，须向用户提供修改后的完整源码（见 [LICENSE](./LICENSE) 第 13 条）